= simple_form_for @cafe do |c|
  = c.input :name
  = c.input :description
  = c.input :street_address
  = c.input :state

  = c.input :editors_note


  = c.label :mp3
  = c.input :mp3, as: :file
  / = c.file_field :mp3


  = c.submit :submit
  = c.input :image, as: :file

/ <%= simple_form_for @user, url: users_path do |form| %>

